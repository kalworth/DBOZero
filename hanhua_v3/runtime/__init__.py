"""Runtime patching modules used by the v3 build/scan pipeline.

These modules were originally developed under ``legacy/tools`` and are the
canonical, actively maintained copies. ``legacy/tools`` keeps thin
compatibility shims so archived scripts can still import the old names.
"""
